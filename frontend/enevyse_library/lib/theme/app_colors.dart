import 'package:flutter/material.dart';

class AppColors {
  // Background Color: Cream / Off-White
  static const Color background = Color(0xFFFDFBF7);

  // Primary Color: Navy Blue (for primary buttons and active states)
  static const Color primary = Color(0xFF494AE2);

  // Auth Header Gradient: Sky Blue
  static const Color headerGradientStart = Color(0xFF494AE2);
  static const Color headerGradientEnd = Color(0xFF49B4E2);

  // Accent Colors: Mocca, Terracotta, and muted soft tones
  static const Color accentMocca = Color(0xFFC0A080);
  static const Color accentTerracotta = Color(0xFFE07A5F);

  // Text Color: Dark Coffee or dark charcoal. No pure black.
  static const Color textPrimary = Color(0xFF2E1F1A);
  static const Color textSecondary = Color(0xFF5A4A42);

  // Neutral Tones
  static const Color surface = Colors.white;
  static const Color border = Color(0xFFEBE6DF);
}
